'use strict';

angular.module('partnerModule').factory('_donation', ['_http', '_cookie', 'appKey',
    function (_http, _cookie, appKey) {
        var service = {};

        var apiController = 'User';
 
       
        return service;
    }]);
