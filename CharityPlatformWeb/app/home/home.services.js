'use strict';

angular.module('homeModule').factory('_home', ['_http', '_cookie', 'appKey',
    function (_http, _cookie, appKey) {
        var service = {};         
       
        return service;
    }]);
